#include <iostream>
using namespace std;

int main()
{
    std::cout << (1 << 0);
    std::cout << (1 << 1);
    std::cout << (1 << 2);
    std::cout << (1 << 3);
    getchar();
    return 0;
}